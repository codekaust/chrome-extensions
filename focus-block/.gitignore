# Packaged extension artifacts
*.zip
*.crx
*.pem
web-ext-artifacts/

# Dependencies (none required, but ignore if ever added)
node_modules/

# OS / editor noise
.DS_Store
*.swp

# Note: icons/*.png are generated by scripts/gen-icons.mjs but are
# committed intentionally so the extension loads without a build step.
